import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build', (WidgetTester tester) async {
    // Basic smoke test
    // Note: Full widget tests require DI initialization
    expect(true, isTrue);
  });
}
